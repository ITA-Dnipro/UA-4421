name: CI

on:
  push:
    branches: 
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop  

jobs:
  backend:
    name: backend
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r startup_gateway/requirements.txt
          pip install -r startup_gateway/requirements-dev.txt

      - name: Run linters
        id: backend_lint
        working-directory: startup_gateway
        continue-on-error: true
        run: |
          set +e
          black --check . || echo "- ❌ Backend lint: Black failed" >> $GITHUB_STEP_SUMMARY
          isort --check-only || echo "- ❌ Backend lint: Isort failed" >> $GITHUB_STEP_SUMMARY
          pylint . || echo "- ❌ Backend lint: Pylint failed" >> $GITHUB_STEP_SUMMARY                

      - name: Run tests with coverage
        working-directory: startup_gateway
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          SECRET_KEY: ${{ secrets.SECRET_KEY }}        
        run: |
          pytest --cov --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          fail_ci_if_error: false
                   

  frontend:
    name: frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Run linters
        working-directory: frontend
        continue-on-error: true
        run: |
          set +e
          npm run lint || "::error::❌ Frontend lint failed. Run 'black .' locally to fix code." >> $GITHUB_STEP_SUMMARY                    

      - name: Run frontend build 
        working-directory: frontend
        run: npm run build
            
